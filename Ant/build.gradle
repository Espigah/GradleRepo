
buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
     
    }
}

ant.importBuild 'build.xml'


task buildTest(dependsOn: build) << {
    println 'Ant:build, completed!'
}